#Make sure that the cluster name and region are the same as the one you created


# Delete EKS Cluster
eksctl delete cluster --name=my-cluster --region=us-west-2     

#Make sure to replace the cluster name with the one you created
#Make sure to replace the region with the one you created
